Mathematical Rules not yet implemented here:

Integration von Identity, Inverse, Square und Power
Integration von Succ, Pred und AddAt

Mathematical Rules implemented here:

Algebra Rules

Name	Rule				Where			Remark
	null == 1				Prod, Quot
	null == 0				Sum, Diff
	null == Id				ConCat
Additive Rules:	
S0	a + 0 = a				Sum				neutral Element
S1	0 + a = a				Sum				neutral Element
S2	a + -b = a - b			Sum
S3	-b + a = a - b			Sum
D0	a - 0 = a				Diff			neutral Element
D1	a - a = 0				Diff			neutral Element
D2	a - -b = a + b			Diff
D3	-b -a = -(a + b)		Diff			not implemented, see C4
D4	0 - a = -a				Diff

Multiplicative Rules (Doubled because of Associative Law):
P0	1 * a = a				Prod			neutral Element
P1	a * 1 = a				Prod			neutral Element
P2	a * 0 = 0				Prod			neutral Element, mixed
P3	0 * a = 0				Prod			neutral Element, mixed
P4	(-1)*a = -a				Prod			This is redundant with P0, P7
P5	a*(-1) = -a				Prod			This is redundant with P1, P7
P6	(1/b)* a = a / b		Prod
P7	a * (1/b) = a/b			Prod
P8	(-a) * b = -(a*b)		Prod
P9	a * (-b) = -(a*b)		Prod
Q0	a / 1 = a				Quotient
Q1	a / a = 1				Quotient
Q2	(1/b)/a = 1 /(a*b)		Quotient
Q3	(-a)/b = -(a/b)			Quotient
Q4	a /(1/b) = a * b		Quotient
Q5	a / (-b) = -(a/b)		Quotient

C0	(-a)(b) = -a(b)			ConCatDerive	get the Sign out of the ConCat
C1	(1/a)(b) = 1/(a(b))		ConCatDerive	get the Inverse out of the ConCat
C2	 -(a-b) = (b-a)			ConCatDerive	
C3	1/(a/b)	=  b/a			ConCatDerive	
C4	-(a+b) = -a-b = -b-a	ConCatDerive	not implemented, see D3
C0	Id(f()) = f()			ConCat
C0	f(Id()) = f()			ConCat
C0	Const(f()) = Const()	ConCat
C0	f(Const()) = Const()	ConCatDerive
C0	f^-1(f()) = Id()		ConCat
C0	f(f^-1()) = Id()		ConCat

	a * b = b * a			Kommutativ
	a + b = b + a			Kommutativ

	a/(b*c) = (a/b)/c		Kommutativ
	a-(b+c) = (a-b)-c		Kommutativ
	
	a+(b+c) = (a+b)+c		Assoziativ
	a*(b*c) = (a*b)*c		Assoziativ
	
	a+(b-c) = (a+b)-c		Assoziativ
	a-(b-c) = (a+c)-b		Assoziativ
	a*(b/c) = (a*b)/c		Assoziativ
	a/(b/c) = (a*c)/b		Assoziativ

	a*(b+c) = ab + ac		Distributiv
	(b+c)*a = ba + ca		Distributiv
	a*(b-c) = ab - ac		Distributiv
	(b-c)*a = ba - ca		Distributiv
//	a/(b+c) = a/b + a/c		Distributiv	Falsch!!
	(b+c)/a = b/a + c/a		Distributiv
//	a/(b-c) = a/b - a/c		Distributiv	Falsch!!
	(b-c)/a = b/a - c/a		Distributiv

Derivation Rules

D0	(f+g)' = f' + g'		SumDerive	 Sum Rule
D1	(f-g)' = f' - g'		DiffDerive	(Sum Rule)
D2	(f*g)' = f'*g+f*g'		ProdDerive	 Product Rule
D3	(f*Const)' = f'*Const				 homogeneous, not necessary
D4	(Const*f)' = Const*f'				 homogeneous, not necessary
D5	(f/Const)' = f'/Const				 homogeneous, not necessary
D6	(Const/f)' = Const*f'				 Falsch!!
D7	(f/g)' =(f'*g-f*g')/g^2	DiffDerive	(Product Rule)
D8	 f(g)' = f'(g) *g'		ConCatDerive Chain Rule
D9	 f^-1' = 1/f'						Derivative of the Inverse, no Inverse Operator!
D10	ln(f())' = f'()/f()					not necessary

Integration Rules

I1	I[f+g] = F + G			SumDerive	 Sum Rule  (additive)
I2	I[f-g] = F - G			DiffDerive	(Sum Rule) (additive)
I3	I[f*g'] = f*g-I[f'*g]				 Product Rule (partial Integration)
I4	I[Const*f] = Const*F	ProdDerive	 homogeneous
I5	I[f*Const] = F*Const	ProdDerive	 homogeneous
I6	I[f/Const] = F/Const	QuotDerive	 homogeneous
//	I[Const/f] = Const/F				 Falsch!!
I7	I[f'/f] = ln (f)		QuotDerive	 logarithmic, see D10
I8	I[f'*f] = f^2/2			ProdDerive	 square
I9	I[f(g())] = F(g())/g'	ConCatDerive	only if g' is constant (g is affine)
I10	I[f(x)]=I[f(x())*x'()]				Substitution (not feasible)